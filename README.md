# Micro Store Template

This is an auto-generated Next.js store template for the **Evolution Engine**.

## Usage
The engine will deploy this template to Vercel and inject the brand data via the `NEXT_PUBLIC_BRAND_DATA` environment variable.
